import 'package:flutter/material.dart';

class Permission_locationWidget extends StatelessWidget {
  const Permission_locationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      child: const Text('Este es un widget de permission_location'),
    );
  }
}
